# Tested environment (Python 3.13). Same implement-the-models philosophy as the sibling
# projects: core scientific stack only, no heavyweight quant/market-making libraries.
numpy>=2.0,<3
scipy>=1.11
pandas>=2.1,<3
matplotlib>=3.7
# Dev / CI:
pytest>=8
ruff==0.12.0
