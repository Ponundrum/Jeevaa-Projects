# Tested environment (Python 3.13). Lower bounds are the versions this project was
# developed and validated against; the code depends on pandas >= 2.0 semantics
# (notably pct_change(fill_method=None) and modern rolling behaviour). Pin exactly
# with `pip freeze` if you need bit-reproducible runs.
numpy>=2.0,<3
pandas>=2.1,<3
pyarrow>=15
scipy>=1.11
statsmodels>=0.14
matplotlib>=3.7
# Dev / CI only:
pytest>=8
ruff>=0.5
